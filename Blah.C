#include <stdio.h>
#include <stdlib.h>

int main(){
	system("matlab -nodisplay -nodesktop -r 'run computationtest(0,7);'");
	//FILE * infile=fopen(argv[1],"r");
	//char str[5];
	//fgets(str,5,infile);
	//printf("%d",atoi(str));
return 0;}
